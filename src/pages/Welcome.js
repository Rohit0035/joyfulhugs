import React from 'react';

const Welcome = ({ name }) => {
  return (
    <div>
      <h1>Welcome, {name}!</h1>
      <p>This is a demo functional component in React.</p>
    </div>
  );
};

export default Welcome;
